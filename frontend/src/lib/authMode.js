/**
 * Production access mode: signed-in homeowners can save projects and pros can manage portfolios.
 */
export const AUTH_UI_ENABLED = true;

/** When true, /project shows the Sharma sample PM board without a projectId. Off for real usage. */
export const PROJECT_HUB_DEMO_MODE = false;
