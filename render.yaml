services:
  - type: web
    name: homemakers-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: HM_AI_IMAGE_API_KEY
        sync: false
      - key: HM_AI_PLAN_API_KEY
        sync: false
      - key: MONGO_URL
        sync: false
      - key: DB_NAME
        value: homemaker
      - key: CORS_ORIGINS
        value: "*"
